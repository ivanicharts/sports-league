import Leagues from '../features/leagues/Leagues';

// In a larger app, this would be where you'd set up routing and code-splitting.
// For this test, we only have one page, so this component is pretty simple.
function AppRouter() {
  return <Leagues />;
}

export default AppRouter;
